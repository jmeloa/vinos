<%= form_with(model: cepa_vino) do |form| %>
  <% if cepa_vino.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cepa_vino.errors.count, "error") %> prohibited this cepa_vino from being saved:</h2>

      <ul>
        <% cepa_vino.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
